/**
 * @module useSimulation
 * @description Backward-compatible re-export of usePortfolioRun as useSimulation.
 */
// useSimulation hook — re-exports from usePortfolio for backward compatibility
export { usePortfolioRun as useSimulation } from './usePortfolio';
