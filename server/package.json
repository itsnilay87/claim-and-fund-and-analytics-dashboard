{
  "name": "claim-analytics-server",
  "version": "0.1.0",
  "description": "Express API server for claim analytics platform — dispatches simulations and manages workspace data",
  "main": "server.js",
  "scripts": {
    "dev": "node --watch server.js",
    "start": "node server.js",
    "build": "echo 'No build step for server'"
  },
  "dependencies": {
    "express": "^4.21.0",
    "cors": "^2.8.5",
    "uuid": "^10.0.0",
    "ajv": "^8.17.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
